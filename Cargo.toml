[workspace]
resolver = "2"
members = ["chess", "cli", "datagen", "engine", "nnue", "utils"]

[profile.release]
opt-level = 3
lto = "fat"
codegen-units = 1
panic = "abort"
strip = true

[profile.dev]
overflow-checks = true
