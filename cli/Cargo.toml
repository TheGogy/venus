[package]
name = "cli"
version = "0.0.1"
edition = "2024"
authors = ["TheGogy"]

[dependencies]
engine = { path = "../engine" }
nnue = { path = "../nnue/" }
clap = { version = "4.5.58", features = ["derive"] }

[features]
tune = ["engine/tune"]
syzygy = ["engine/syzygy"]
embed = ["nnue/embed"]
