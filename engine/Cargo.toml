[package]
name = "engine"
version = "0.5.0"
edition = "2024"
authors = ["TheGogy"]

[dependencies]
chess = { path = "../chess" }
utils = { path = "../utils" }
nnue = { path = "../nnue" }

[features]
tune = []
