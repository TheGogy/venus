[package]
name = "chess"
version = "0.3.1"
edition = "2024"
authors = ["TheGogy"]

[lib]
doctest = false

[dependencies]
arrayvec = "0.7.8"
clap = { version = "4.6.7", features = ["derive"] }
ctor = "1.0.13"
utils = { path = "../utils" }

[features]
full_tests = []
