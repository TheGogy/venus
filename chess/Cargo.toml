[package]
name = "chess"
version = "0.3.1"
edition = "2024"
authors = ["TheGogy"]

[dependencies]
ctor = "0.6.1"
utils = { path = "../utils" }
clap = { version = "4.5.58", features = ["derive"] }

[lib]
doctest = false

[features]
full_tests = []
